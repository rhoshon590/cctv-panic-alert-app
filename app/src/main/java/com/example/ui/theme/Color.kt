package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// High Density Theme Colors (Warm Light M3 Design Theme)
val HighDensityBackground = Color(0xFFFDF8F6)  // Warm peach screen background
val HighDensityPrimary = Color(0xFF6750A4)     // M3 deep purple core / triggers
val HighDensityOnPrimary = Color.White
val HighDensitySecondary = Color(0xFF21005D)   // High density dark accent text
val HighDensityPrimaryContainer = Color(0xFFEADDFF) // Highlight accent backgrounds (pills, badges)
val HighDensityOnPrimaryContainer = Color(0xFF21005D)
val HighDensitySurface = Color.White           // Cards background
val HighDensityOnSurface = Color(0xFF1D192B)   // Card bold body texts
val HighDensitySurfaceVariant = Color(0xFFF3EDF7) // Secondary card boxes
val HighDensityOnSurfaceVariant = Color(0xFF49454F) // Sub-text in dark variant gray
val HighDensityOutline = Color(0xFFCAC4D0)        // Material borders/grid outlines

// Soft Urgent Panic Container color pairings (Soft Red Theme)
val SoftPanicBg = Color(0xFFF2B8B5)       // Soft urgent light peach-red
val SoftPanicTextDark = Color(0xFF410002) // Red-950 solid text headline
val SoftPanicTextBody = Color(0xFF601410) // Red-900 thin descriptive text
val SoftPanicBadgeBg = Color(0xFF8C1D18)  // Red badge indicator backing
val SoftPanicBadgeText = Color.White

// Real-Time Safety status colors
val StatusGreen = Color(0xFF2E7D32)       // Secure GPS green
val WarningOrangeBg = Color(0xFFFFF3E0)   // Silent mode warn background
val WarningOrangeText = Color(0xFFE65100) // Silent warning text accent
val AlertUrgentRed = Color(0xFFD32F2F)    // Activated alarm urgent red

// Keeping existing constants for binary capability if referenced elsewhere
val CharcoalGrey = Color(0xFFFDF8F6) // Redirect to light background style
val DarkSurface = Color(0xFFFFFFFF)  // Redirect to light card style
val BrightRed = Color(0xFFD32F2F)
val SoftRed = Color(0xFFF2B8B5)
val SignalCyan = Color(0xFF6750A4)
val SafeGreen = Color(0xFF2E7D32)
val SlateText = Color(0xFF49454F)
val GlassWhite = Color(0xFFEADDFF)
val AlertOrange = Color(0xFFE65100)
